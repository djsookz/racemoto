<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_track_metric_speed"
    android:padding="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tvDialogTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/garage_profile_image"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btnCloseImageDialog"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/bg_icon_button"
            android:src="@drawable/ic_close"
            app:tint="@android:color/white"
            android:padding="8dp" />

    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/llSelectImage"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="14dp"
        android:text="@string/garage_choose_from_library"
        android:textColor="@android:color/white"
        app:cornerRadius="18dp"
        app:backgroundTint="@color/accent_color" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/llRemoveImage"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="10dp"
        android:text="@string/garage_remove_photo"
        android:textColor="@android:color/white"
        app:cornerRadius="18dp"
        app:strokeColor="#40FF3C3C"
        app:strokeWidth="1dp"
        app:backgroundTint="#12FF3C3C"
        android:visibility="gone" />

</LinearLayout>

